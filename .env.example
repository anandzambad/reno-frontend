# Browser-restricted Google Maps Platform key. Never put server secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Backend base URL.
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Temporary local/demo identity only. Replace with authenticated session context before production booking.
NEXT_PUBLIC_DEMO_CUSTOMER_ID=
